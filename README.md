# Hoops_Lab
HoopsLab: NBA + EuroLeague Intelligence Platform

## Scaffold Status

Monorepo scaffold is initialized with:
- `apps/api` (Cloudflare Worker skeleton)
- `apps/web` (Next.js skeleton)
- `data/etl` (Poetry ETL skeleton)
- `data/schema` (initial SQL placeholder)
- `docs` (documentation placeholders)
- `.github/workflows` (CI and ETL placeholders)

Implementation will proceed after manual setup verification.
