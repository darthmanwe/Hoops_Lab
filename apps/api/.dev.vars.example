# Copy to .dev.vars for `wrangler dev`. Do not commit real secrets.
#
# The Worker currently needs no secrets: it reads only D1 and KV, both of which
# are bound in wrangler.toml per environment.
#
# Non-secret configuration (APP_ENV, DATA_SNAPSHOT) lives in wrangler.toml
# under [env.*].vars rather than here.
