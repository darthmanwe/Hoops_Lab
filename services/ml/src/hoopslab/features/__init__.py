"""Model-ready frames, built from gold.

Every feature the models consume is constructed here, so there is exactly one
place where a feature definition can live and exactly one place train/serve
skew could be introduced.
"""
